# Figures

논문에 삽입할 publication figure와 figure별 source data를 둔다.

파일명은 `F{번호}_{짧은_설명}.{svg,png,pdf,csv}` 형식을 사용한다. 회의용 그림을
그대로 복사하지 않고, 논문용으로 채택한 그림만 이 디렉토리에 생성한다.

현재 F1-F3의 용도, caption 초안과 provenance는 `F1_F3_figure_register.md`에서
관리한다. F3의 generated source table은 `source_data/`에 둔다.
